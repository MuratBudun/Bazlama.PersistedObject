# PersistedObject Encryption Settings
# Copy this file to .env and set your own values

# Encryption key for JSON data (used with ApiKey model)
# Generate a secure key: python -c "import secrets; print(secrets.token_urlsafe(32))"
PERSISTED_OBJECT_ENCRYPTION_KEY=your-secret-encryption-key-here

# Salt for key derivation (PBKDF2)
# Generate a salt: python -c "import secrets; print(secrets.token_urlsafe(16))"
PERSISTED_OBJECT_ENCRYPTION_SALT=your-salt-value-here

# Example values (DO NOT USE IN PRODUCTION):
# PERSISTED_OBJECT_ENCRYPTION_KEY=example_key_12345_do_not_use_in_production
# PERSISTED_OBJECT_ENCRYPTION_SALT=example_salt_67890_do_not_use_in_production